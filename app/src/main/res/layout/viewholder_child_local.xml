<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/sticker_layout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/sticker_img"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"/>

    <ImageView android:id="@+id/sticker_lock"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="fitXY"
        android:src="@drawable/kika_sticker_app_ic_lock"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"/>

    <ProgressBar
        style="?android:attr/progressBarStyle"
        android:id="@+id/sticker_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:indeterminateTint="#20000000"
        android:progressDrawable="@drawable/kika_sticker_app_loading"
        android:indeterminateTintMode="src_atop"
        android:layout_gravity="center"
        android:visibility="visible" />
</RelativeLayout>